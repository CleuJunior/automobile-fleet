server:
  port: 8000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/project_auto
    username: postgres
    password: postgres
  jpa:
    hibernate:
#      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  flyway:
    enabled: false